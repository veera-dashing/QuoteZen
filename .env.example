# Postgres connection used by packages/db (Prisma) and apps/api
DATABASE_URL="postgresql://quotezen:quotezen@localhost:5432/quotezen?schema=public"

# API
API_PORT=4000
JWT_SECRET="change-me-in-production"
JWT_EXPIRES_IN="12h"

# Web → API base URL
NEXT_PUBLIC_API_URL="http://localhost:4000"
